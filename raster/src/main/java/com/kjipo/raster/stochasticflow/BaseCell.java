package com.kjipo.raster.stochasticflow;

import com.kjipo.raster.AbstractCell;

public class BaseCell extends AbstractCell {
    protected BaseCell(int row, int column) {
        super(row, column);
    }
}
